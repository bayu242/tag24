import type { TagPayloadVersion } from "./types";

export const TAG_PAYLOAD_VERSION: TagPayloadVersion = "1";

export const TAG_QUERY_PARAM = "d" as const;

export const TAG_COMPRESSION = "deflate-raw" as const;

export const TAG_BASE64URL_PADDING = false;

export const TAG_LINK_PLACEHOLDER = "{value}" as const;
